{
  "name": "shredder",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start --filter=@shredder/api --filter=@shredder/dashboard --filter=@shredder/worker",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.4.4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
